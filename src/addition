(ns addition)

;write a recusive function to add numbers
(defn add-nums-recursively [x & more]
  (loop [y (first more) z (rest more)]
    (println "Z is " z)
  (if (zero? (count z))
    (+ x y)
    (recur (+ x y) (z))
  )
)
)